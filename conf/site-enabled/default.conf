server {
    listen 8080;
    location / {
	root /usr/local/openresty/nginx;
        default_type text/html;
	    content_by_lua_file './conf/lua/init.lua';
        # content_by_lua '
        #     local args = ngx.req.get_uri_args();
        #     ngx.say(ngx.req);
        # ';
    }
}
